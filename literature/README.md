# Literature

Reference material used for the control benches of this repository:

- `predictive_control_in_power_electronics.pdf` — model predictive control applied to power
  converters and drives (basis of the MPC benches in `psm_sensorless_ctrl/`).
